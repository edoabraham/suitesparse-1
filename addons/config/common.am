
pkgconfigdir = $(libdir)/pkgconfig
pkgconfig_DATA = $(top_builddir)/$(PACKAGE_NAME).pc

EXTRA_DIST = README.txt

MAINTAINERCLEANFILES = \
        aclocal.m4 \
        config.h \
        config.h.in \
        configure \
        Makefile.in \
	stamp-*
